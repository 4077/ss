<div class="{__NODE_ID__} focusable {FOCUS_CLASS}" instance="{__INSTANCE__}">

    <div class="a">
        <div class="changes_types_filter">
            <!-- change_type_filter -->
            {BUTTON}
            <!-- / -->
        </div>
        <div class="paginator">
            {PAGINATOR}
        </div>
    </div>

    <div class="b">
        <table>
            <!-- change -->
            <tr class="change row {INSTALLED_CLASS} {REMOTE_CAT_CHANGED_CLASS}" n="{N}" change_id="{CHANGE_ID}" product_id="{PRODUCT_ID}">
                <td class="import_run_number" width="1">{IMPORT_RUN_NUMBER}</td>
                <td class="date" width="1">{DATE}</td>
                <td class="status {STATUS_CLASS}" width="1">{STATUS}</td>
                <td class="name">{NAME}</td>
            </tr>
            <!-- / -->
        </table>
    </div>

</div>
