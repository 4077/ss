<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <table class="props">
        <!-- prop -->
        <tr class="prop" prop_number="{NUMBER}">
            <td class="label" hover="hover" width="100">
                {DELETE_BUTTON}
                {LABEL_TXT}
            </td>
            <td class="value" hover="hover">{VALUE_TXT}</td>
        </tr>
        <!-- / -->
    </table>

    {ADD_BUTTON}

</div>
