<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <div class="components">
        <!-- component -->
        <div class="component {ENABLED_CLASS}" pivot_id="{PIVOT_ID}">
            <div class="buttons">
                {TOGGLE_BUTTON}
                {TOGGLE_PINNED_BUTTON}
                {PIVOT_DATA_BUTTON}
                {DEV_CP_BUTTON}
            </div>
            <div class="name">
                {NAME}
            </div>
            <div class="hover_buttons">
                {DELETE_BUTTON}
            </div>
        </div>
        <!-- / -->
    </div>

    <div class="buttons">
        {ADD_BUTTON}
    </div>

</div>
