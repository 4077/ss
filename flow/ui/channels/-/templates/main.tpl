<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <canvas></canvas>

    <!-- node -->
    <div class="node" node_id="{ID}">
        {CONTENT}
    </div>
    <!-- / -->

    <div class="contextmenu">{CONTEXTMENU}</div>
    <div class="node_contextmenu">{NODE_CONTEXTMENU}</div>
    <div class="channel_contextmenu">{CHANNEL_CONTEXTMENU}</div>

</div>
