<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <div class="divisions">
        <div class="label">Подразделения</div>
        <div class="grid">
            {DIVISIONS}
        </div>

        {DIVISION_CREATE_BUTTON}
    </div>

    <div class="warehouses">
        <div class="label">Склады</div>
        <div class="grid">
            {WAREHOUSES}
        </div>

        {WAREHOUSE_CREATE_BUTTON}
    </div>

    <div class="groups">
        <div class="label">Группы</div>
        <div class="grid">
            {GROUPS}
        </div>

        {GROUP_CREATE_BUTTON}
    </div>

</div>
