<div class="{__NODE_ID__}" instance="{__INSTANCE__}">
    <style type="text/css">
        .{__NODE_ID__} .toggle_button.enabled {
            background-color: #{BG_COLOR}
        }

        .{__NODE_ID__} .toggle_button.enabled .icon {
            color: #{FG_COLOR}
        }
    </style>

    {TOGGLE_BUTTON}

    <!-- enabled -->

    <div class="inputs">
        <div class="colors">
            <div class="bg color">{BG_TXT}</div>
            <div class="fg color">{FG_TXT}</div>
        </div>
        <div class="columns">
            {COLUMNS_TXT}
        </div>
    </div>
    <!-- / -->

</div>
