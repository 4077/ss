<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <div class="table">
        <!-- cell -->
        <div class="row" coord="{COORD}">
            <div class="cell coord">{COORD_TXT}</div>
            <div class="cell value">{VALUE_TXT} {DELETE_BUTTON}</div>
        </div>
        <!-- / -->
    </div>

    {CREATE_BUTTON}

</div>
