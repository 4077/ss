<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <div class="nav">
        <div class="division_selector">
            {DIVISION_SELECTOR}
        </div>
        <a class="divisions_route_button" href="{DIVISIONS_ROUTE}">
            <div class="icon fa fa-cog"></div>
        </a>
    </div>

    <div class="tabs">
        <!-- tab -->
        {BUTTON}
        <!-- / -->
    </div>

    <div class="content {CLASS}">
        {CONTENT}
    </div>

</div>
