<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <div class="top_bar">
        <a class="name" href="{DOWNLOAD_URL}" title="{ID}">{NAME}</a>

        {DETECT_IMPORTERS_BUTTON}
    </div>

    <div class="report">
        {REPORT}
    </div>

</div>
